# الاختبارات — كيف تشغّليها، وشو انكشف فيها

هالملف بيحكي شغلتين: **كيف تشغّلي كل شي**، و**شو فشل فعلياً** أثناء البناء.
القسم التاني هو الأهم — كل بغ هون انكشف باختبار، ومعظمهن ما كانوا يبيّنوا
بقراءة الكود.

---

## التشغيل

```bash
# مرة وحدة على الجهاز
apt-get install -y postgresql
npm install -g deno

# كل شي — من قاعدة فاضية
./supabase/tests/run.sh && ./tests/run.sh
```

بيبني Postgres محلي من الصفر، بيشغّل الترحيلات والبذور، وبعدين ١١ مشغّل.
ما بده Supabase ولا إنترنت ولا مفتاح API.

### مشغّل واحد لحاله

| الأمر | شو بيفحص |
|---|---|
| `./supabase/tests/run.sh` | المخطط + كل اختبارات SQL |
| `./tests/tools.sh` | سكربت البناء، حارس التسريب، أدوات الرفع |
| `python3 tests/export.py` | المصدّر: JSON ← SQL ← قاعدة بيانات |
| `node tests/parse.mjs` | المحلّل: حالات حدّية ومدخلات عدائية |
| `node tests/markup.mjs` | round-trip الصيغة على الـ١٦ امتحان |
| `node tests/browser.mjs` | تطبيق الطلاب بـChromium |
| `node tests/xss.mjs` | حقن XSS بالتطبيق واللوحة |
| `node tests/import.mjs` | استيراد من الطرف للطرف |
| `node tests/admin.mjs` | لوحة التحكّم بـChromium |
| `deno run --allow-all tests/edge/run.ts` | Edge Function بـDeno الحقيقي |

> اختبارات المتصفّح بدها `node_modules` مربوطة بالعالمية:
> `ln -sfn "$(npm root -g)" node_modules` — و`tests/run.sh` بيعملها لحالها.

### ملفات SQL

| الملف | شو بيفحص |
|---|---|
| `01_access_and_grading.sql` | الصلاحيات، قفل الحلول، التصحيح، سقف الأجهزة |
| `02_admin.sql` | إجراءات اللوحة وسجلّ التدقيق |
| `03_review.sql` | صناديق التكرار المتباعد والفواصل |
| `04_writing.sql` | التعبير الكتابي: الحصّة وحساب النقاط |
| `05_permissions.sql` | تدقيق: شو بيقدر يعمل طالب عادي فعلياً |
| `06_regressions.sql` | البغّات يلي انكشفوا — تا ما يرجعوا |

**المجموع: ٣٠٨ تأكيد.**

---

## شو فشل فعلياً

### 🔴 ١. الكود بينعاد تنفيذه ويمدّد كل مرة

**خطر مالي مباشر.** الطالب بيدخّل كوده، وبعدين بيدخّله مرة تانية — وبينمدّد ٣٠
يوم زيادة. ١٢ إدخال = سنة مجاناً.

كتبت `if v_code.redeemed_at is not null and v_code.redeemed_by <> v_user` عشان
النداء يكون آمن لو التطبيق أعاده. بس السماح لنفس المستخدم خلّى التمديد يتراكم.

**الإصلاح:** الكود مرة وحدة فعلاً. إعادة النداء بترجّع `ok:true, already:true`
مع نفس تاريخ الانتهاء — آمنة للإعادة وبلا تمديد.
**الاختبار:** `06_regressions.sql` — عشر إعادات، ولا يوم زيادة.

### 🔴 ٢. حصّة التصحيح ما كانت تنفرض أبداً

نافذة العدّ كانت `(نهاية الاشتراك − ٣٠ يوم)`. مع اشتراك سنوي هاد تاريخ
**بالمستقبل**، فالعدّ بيطلع صفر دايماً والحصّة ما بتنفرض. يعني تكلفة API بلا سقف.

**الإصلاح:** ٣٠ يوم متدحرجة من اليوم — ما بتصير بالمستقبل مهما كان الاشتراك.
**الاختبار:** اشتراك ٣٦٥ يوم بحصّة ٢ — الطلب التالت انرفض.

### 🟡 ٣. المحلّل كان بيقبل قسم بلا صيغة أو بصيغة مجهولة

`Format:` ناقصة أو `Format: quantum` كانوا يمرقوا **بلا ولا تحذير**، والنشر
بيصير متاح. النتيجة: امتحان بقاعدة البيانات ما بينعرض بالتطبيق.

**الإصلاح:** تحذير لكل حالة، والاتنين صاروا يقفلوا زرّ النشر.
**الاختبار:** `tests/parse.mjs`.

### 🔴 ٤. `[hidden]` كانت ناقصة كلياً من الـCSS

`elem.hidden = true` ما كان يخفي **ولا عنصر** بكل التطبيق — لأن كل زرّ وشريط
عنده `display` صريح بالقاعدة، وهاد بيغلب السمة. زرّ الرجوع، المؤقّت، التنبيهات،
زرّ التصحيح — كلهن.

انكشف بتأكيد صغير: «الزرّ اختفى بعد النجاح».

### 🟡 ٥. `Extra:` كان بيبلع باقي الملف

بصيغة اللصق، كتلة `Extra:` ما كان إلها نهاية — فبتاكل الأقسام والأسئلة يلي
بعدها. انكشف بالـround-trip على الـ١٦ امتحان.
**الإصلاح:** بتنتهي لما تتوازن الأقواس.

### 🟡 ٦. سطر `Aufgaben:` كان بينحسب فقرة من النص

بوضع `Text:`، السطر يلي بينهي النص كان بينبلع كأنه جزء منه.
**الإصلاح:** قائمة حدود فاصلة (عنوان، سؤال، تسمية مبدّلة للوضع).

### 🟡 ٧. `Fehlend` بالكتلة ما بينستنتج

عدد الأسئلة الناقصة بيشمل **أقسام غايبة كاملة** — معلومة تحريرية ما بيقدر
المحلّل يعرفها من الموجود. النموذج ١٢ فشل بالـround-trip بسببها.
**الإصلاح:** صارت تنكتب صراحةً.

### 🟠 ٨. RLS بتحدّد الصفوف، بس ما بتعطي صلاحية على الجدول

أول مرة شغّلت الاختبارات على Postgres حقيقي، **كل استعلام فشل**: `permission
denied for table tests`. كتبت السياسات بس نسيت `GRANT`. الاتنين لازم.

لو رحت على Supabase مباشرةً كنتِ وقعتِ بحيط بلا سبب واضح.

### 🟡 ٩. حذف حساب أدمن كان بينحبس

`imports.created_by` كان مرجع مانع، فحذف الأدمن مستحيل — ولو صار، كان بيجرّ
سجلّ التدقيق معه. صارت `on delete set null`: التاريخ بيعيش بعد الحساب.

### 🟡 ١٠. `screenHome()` كان بيستدعي حاله بلا نهاية

كان يجيب عدد الأخطاء ويعيد الرسم **بلا شرط** — وكل رسم بيجدول رسم تاني.
الصفحة بتموت. انكشف لما المتصفّح انهار بالاختبار.
**الإصلاح:** بيعيد الرسم بس إذا العدد تغيّر.

### 🟡 ١١. `bundle.py` كان ميت وخطر

كان يستبدل نداءات `fetch` ما عادت موجودة — يعني بينتج ملف مكسور **بلا ما
يفشل**. وأصلاً فكرته (ملف واحد فيه كل الامتحانات) صارت تناقض الحماية.
انشال.

### 🟡 ١٢. نسخة SDK وZod غلط

أول ما فحصت الـEdge Function بـDeno: الـSDK يلي ثبّته ما بيعرف
`thinking: adaptive` ولا `output_config.effort`، و`zodOutputFormat` بده Zod 4
مو 3. ما كان يبيّن غير بفحص الأنواع الفعلي.

---

## أخطاء بالاختبارات نفسها

**تلاتة، وكل واحد فيهن كان بيعطي ثقة كاذبة:**

### ✗ «٦ ثغرات أمنية» ما كانت موجودة

أول تدقيق صلاحيات حسبت **«ما رمى استثناء» = «الهجوم نجح»**. بس `UPDATE` يلي
بتصدّه RLS ما بيرمي خطأ — بيعدّل صفر صفوف وبيرجّع نجاح.

فقلت إن الطالب بيقدر يقرا الحلول، يرقّي حاله لأدمن، ويشطب سجلّ التدقيق. **ولا
وحدة صحيحة.** الفحص الصح بيقارن **الحالة بعد المحاولة**.

### ✗ تأكيد كتبته بـ`|| true`

اختبار «النص المسموع بيبيّن بعد التسليم» كان بينجح دايماً لأني حطّيت `|| true`.
الميزة نفسها ما كانت موجودة. أضفتها فعلياً وشلت الـ`|| true`.

### ✗ اختبارات بتعتمد على ترتيب تشغيلها

`tests/import.mjs` كان بيتّكل على حساب أدمن خلّفه `02_admin.sql`. لما
`03_review.sql` صار يمسح الملفات الشخصية، وقع. صار كل اختبار يبني حالته بنفسه.

---

## شو **مو** مغطّى

بصراحة، تا ما تتفاجئي:

- **ولا شي انجرّب على Supabase المستضاف.** كل شي على Postgres محلي. نصوص
  PostgREST بـ`api.js`، الدخول المجهول، وتوقيع روابط Storage — هدول بيعتمدوا
  على الخدمة نفسها. **متوقّع تعديلات صغيرة أول تشغيل.**
- **ولا نداء حقيقي لـClaude.** الـEdge Function انجرّبت بـDeno الحقيقي بس
  بـClaude مزيّف. الشكل صحيح؛ جودة التصحيح الفعلية ما انقيست.
- **ما في اختبار حِمل.** ولا فحص لسلوك عدة أجهزة بنفس اللحظة.
- **ما في CI.** الاختبارات بتنشغل بالإيد.
- **`redeem_code` بلا حد محاولات.** فضاء الأكواد ٣١⁸ ≈ ٨×١٠¹¹ وSupabase عندها
  حدودها، بس ما في شي كتبناه إحنا بيوقف تخمين متكرّر.
- **`tools/build.py`** (قراءة الـPDF) بلا اختبارات — بيشتغل بس لما تعيدي
  التوليد من الـPDF الأصلي.

---

## لما تضيفي شي جديد

١. **اكتبي الاختبار يفشل أول.** كل بغ فوق انكشف لأن الاختبار حاول شي حقيقي.
٢. **افحصي الأثر مو الاستثناء.** غلطة رقم ١ فوق كلّفتني تحقيق كامل بثغرات وهمية.
٣. **ولا `|| true` ولا `catch {}` فاضي** بالتأكيدات.
٤. **كل اختبار ينظّف حالته بالبداية** — الترتيب ما لازم يقرّر النجاح.
٥. **بغ انصلح = اختبار انحدار** بـ`06_regressions.sql`.
